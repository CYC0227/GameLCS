import java.util.*;

public class GameLCS {
  

	public static void main(String[] args) {
		/*
		Scanner sc = new Scanner(System.in);
		String answer; 
		int gameLevel = 1;//¼öÁ¤ ÇÊ
		int gameType = 1;
	//	QuestionGenerator qg =  new QuestionGenerator(gameLevel, "grape");
		QuestionGenerator qg =  new QuestionGenerator(gameLevel, gameType);
				
	//	System.out.println(qg.getQuestion());
		
		answer = sc.nextLine();
		qg.setAnswer(answer);

	//	System.out.println(qg.getAnswer());
		
	//	String X = qg.getQuestionOriginal();
		String Y = qg.getAnswer();
		
		int m = X.length();
		int n = Y.length();
		
		int[][] T = new int[m + 1][n + 1];

		AlgorithmLCS.LCSLength(X, Y, m, n, T);
		
		System.out.println((AlgorithmLCS.LCS(qg.getQuestionOriginal(),qg.getAnswer(),m,n,T)));
	}*/
	}
	}
